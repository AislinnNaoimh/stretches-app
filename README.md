# Stretches App

A small Next.js starter app for planning and moving through a stretch routine.

## Getting Started

Install Node.js, then install dependencies:

```bash
npm install
```

Run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser.
